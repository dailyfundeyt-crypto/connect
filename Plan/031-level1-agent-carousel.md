# Level 1 — agent cover-flow

Open creative stage for a company:

- Dark liquid backdrop
- Agent windows in a horizontal cover-flow (center focused, neighbors peek)
- **← / →** (and chevron buttons) switch agent → agent
- Floating composer at the bottom targets the focused agent
- Active agent synced to `?level=1&agent=`
