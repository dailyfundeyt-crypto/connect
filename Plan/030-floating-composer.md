# Floating Composer bar

Home uses a Cursor-style dark floating Composer:

- Always `fixed`, default centered mid-viewport
- Drag the top chrome (“Composer · ziehen”) to reposition
- Position persisted in `localStorage` (`connect.floating-composer`)
- Two-row layout so prompt text never collides with mic/send
